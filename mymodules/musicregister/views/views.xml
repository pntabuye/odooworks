<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="musicregister.list">
      <field name="name">musicregister list</field>
      <field name="model">musicregister.musicregister</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="musicregister.action_window">
      <field name="name">musicregister window</field>
      <field name="res_model">musicregister.musicregister</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="musicregister.action_server">
      <field name="name">musicregister server</field>
      <field name="model_id" ref="model_musicregister_musicregister"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <menuitem name="musicregister"
      id="musicregister.menu_root"
      web_icon="musicregister,static/description/icon.png"/>
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="musicregister.menu_1" parent="musicregister.menu_root"/>
    <menuitem name="Menu 2" id="musicregister.menu_2" parent="musicregister.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="musicregister.menu_1_list" parent="musicregister.menu_1"
              action="musicregister.action_window"/>
    <menuitem name="Server to list" id="musicregister" parent="musicregister.menu_2"
              action="musicregister.action_server"/>
    -->
  </data>
</odoo>
